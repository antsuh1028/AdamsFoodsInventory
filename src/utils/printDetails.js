//TODO:: Create Print function that gets the info, and places them into the cells 
// of the excel file "STORAGE PLT TAG" and then prints them. The number at the top should increment per print. (01, 02, 03, ...)

function printDetails(inputs, setShowDetails, setItems, toast) {
}

export default printDetails;